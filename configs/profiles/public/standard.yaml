# Public Test Profile
# Very reliable ground-truth tests from dataset

# Bayesian parameters (very high reliability)
alpha: 0.001   # Very low false positive rate
beta: 0.1
gamma: 0.1
learning_rate: 0.05
